#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: This program prints a string with proper grammar using printf,
 *              followed by a new line.
 *
 * Return: 0 (Equals Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
